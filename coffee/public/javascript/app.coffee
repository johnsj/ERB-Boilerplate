define [  'jquery'
          'underscore'
          'backbone']
, ($, _, Backbone) ->
  initialize = () ->
    console.log "Backbone App Ready for development"
    return

  return {initialize: initialize}

define(['jQuery', 'Underscore', 'Backbone', 'Router'], function($, _, Backbone, Router) {
  var initialize;
  initialize = function() {
    Router.initialize();
  };
  return {
    initialize: initialize
  };
});
